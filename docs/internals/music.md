# Internals: music library, tagging and the picker

Why the library, tagging and track-selection code is shaped the way it is. The
short invariants are in the root `CLAUDE.md`; read the matching section here
before changing any of them — several record a measured property of a real
library (an 11k-track station) that is not obvious from the code.

Covers: Subsonic ingest, multi-value genres, scene consolidation, era-year
resolution, the similarity/embedding text, audio-mood calibration and propagated
energy, LLM provider wiring, the tagging stamp, library coverage, the
artist-variety guard, the album cooldown, full playlist rotation, minimum
track length, seed-vs-pick,
dead-air trim, festivals, listener request hardening, likes, and the blocklist
rules.

- **Subsonic**: keep using `getAnnotatedUri` for anything going to Liquidsoap — raw URLs lose metadata until ID3 arrives, and the `on_metadata` hook needs `subsonic_id` for `/cover/:id` artwork.
- **Genres are multi-value**: `subsonic.songGenres(song)` is the single ingest normaliser (OpenSubsonic `genres: [{name}]` array → `string[]`, legacy scalar fallback). library.db stores `genres` as a JSON array; the scalar `genre` column is GENERATED (genres[0], indexable) — write `genres`, never `genre`. Matching is any-of via `show-filter.ts trackGenres/genreMatches`; LLM-facing shapes carry all tags comma-joined in the `genre` field. **Genre matching is one-directional**: a track's tag may REFINE the show's genre (a "Punk" show admits a `Punk Rock` track) but never broaden it (a "Pop Punk" show rejects a plain `Pop` track), and containment is word-boundary aligned so a "Rap" show doesn't pull `Trap`. The reverse direction is what let strict shows leak broad tags — don't reintroduce it. Note the resolver `subsonic.resolveGenreName` is still deliberately loose both ways: it maps free text to a real library tag for listener requests and server-side fetches, so a show whose genre isn't a library tag can still broaden there (see issue for the fetch-hint/match-target split).
- **Scene vocabulary (#1577)**: "scene" is what the Observatory calls a genre tag and the word the operator asked in terms of, so the consolidation tool uses it; the storage is `tracks.genres`. After a full pass a real catalogue carries near-duplicate spellings that each hold a handful of tracks, and the Tagging panel's **Scene vocabulary** section lists every distinct value with its count (`library-db/scenes.ts sceneVocabulary`, a `json_each` walk over the mirror — uncached and NOT scoped to tagged rows, since an untagged track's genre is as noisy as a tagged one's) and merges a ticked set into one survivor. **A merge is two halves and both are required**: the REWRITE (`db.mergeScenes`, one transaction over just the rows carrying a retired value — the dedupe and the order-preserving rewrite are done row-at-a-time in JS so they state the same rules `songGenres` applies at ingest, once, rather than twice in two languages; the scalar `genre` column is GENERATED so it follows, and a rewritten row with a text vector is marked `text_vector_dirty` because the genre line is in the embed text) and the durable RULE (`music/scene-vocab.ts`, `state/scene-aliases.json`, applied by `subsonic.songGenres`). **Without the rule the feature is a no-op within one cycle**: the mirror is rebuilt from Navidrome on every walk and `upsertTrackMeta` writes the walked genres over the stored ones, so a merge with no memory is undone by the next tag pass — which is the pass that produced the noise. `library.consolidateScenes` is where the two are paired, and it records the rule FIRST so a rolled-back rewrite still leaves the fold in place. The alias map is kept FLAT (a target that is itself retired resolves through; existing rules pointing at a source are repointed) so the lookup is one hop and a cycle cannot be stored — **except on a REVERSAL**, where the target resolves back onto a value this same merge is retiring: resolving through there cancels the merge against itself, every source drops out as a self-merge, and the operator gets a 200 naming the spelling they were trying to replace (#1580 review), so the typed target stands and the old rule is repointed instead. A rule's `from` is a fold KEY and its `to` a stored spelling, so **the two sides may look identical and still do work** ("rock" → "Rock" canonicalises every case and spacing variant); neither `planAliases` nor the file loader may drop a rule for reading like an identity, because that is exactly what a pure case merge is and dropping it let the next walk write the retired spelling straight back. For the same reason a source counts as "already the target" only when it matches VERBATIM — the case-insensitive test rewrote nothing on a case-only merge while reporting success. At the cap the OLDEST rule goes, never the one just recorded: a head slice discarded the new rule while its row rewrite still committed, which is a merge that unwinds at the next walk. The fold is case- and whitespace-insensitive but **never punctuation-insensitive** — "Hip Hop" and "Hip-Hop" are different labels and which survives is the operator's call, and folding them in the listing would hide the thing they came to fix. Sources match the EXACT stored value the operator ticked, so a rule can still be broader than the rewrite that came with it (an untickled case variant folds at the next walk rather than now) — that gap is the design, not a bug. The trim-and-dedupe both halves apply is `scene-vocab.dedupeScenes`, stated once and shared by `applyAliases` (ingest) and `mergeScenes` (rewrite). Loading is lazy and SYNCHRONOUS because `songGenres` runs in two processes (controller + tagger child) and a boot hook one of them forgets is a silently inert feature. This is post-hoc curation and touches nothing the tagger asks for, so `TAGGER_CONTRACT_VERSION` stays put. Forgetting a rule stops future folds and leaves the rewritten rows alone — several spellings became one, and which row had which is exactly what the merge discarded. Pinned by `scripts/scene-vocab.test.ts` (real SQLite: the generated column, the dedupe, the dirty-vector marking and the walk round trip would all pass a pure test while broken).

- **What a merge orphans, named before it runs (#1593)**: retiring a value is right — the operator asked for it — but a show genre filter, a never-play rule or a sync playlist's `knobs.genres` still naming the retired spelling then stops selecting it, with no error and no visible cause: the library is correct and the schedule quietly stops working. `music/scene-references.ts` scans those stores and `POST /library/scenes/references` answers the same question the merge will, from the same call, so the preview in the Scene vocabulary section and the merge response cannot disagree. **Naming the affected shows is the whole feature** — a generic "this may affect filters" is the non-advice the operator already assumed. **The orphan test is two questions asked at different strengths.** Does the filter value NAME the retired spelling (a MUTUAL match — each side covers the other, which under `genreMatches`' boundary containment can only happen when the two normalise equal), and does it still catch the survivor (asked ONE-WAY, the direction the pick paths ask it)? The asymmetry is the whole correctness of the thing: a `Punk` show CATCHES `Punk Rock` by refinement, so a one-way first question reported it orphaned on any merge moving punk rock elsewhere while every track tagged plain `Punk` still matched — that show has NARROWED, not broken, and narrowing happens on nearly every merge touching an overlapping genre, which is exactly the generic non-advice being replaced. So `rock` → `Rock`, `Hip-Hop` → `Hip Hop` and (against a `Punk` show) `Punk Rock` → `Post-Punk` or `Punk Rock` → `Downtempo` all produce no warning, while `trip-hop` → `Downtempo` against a `Trip Hop` show does. Both questions go through the predicate the matching side really runs, never a local comparison, and the target resolves through `planAliases` first so a typed target that is itself already retired is judged as the value that will really survive. **`tag` rules are scanned too, with their own predicate**: `field: 'tag'` matches `trackAllTags` (genres ∪ moods ∪ audio moods ∪ Last.fm tags) by `normText` EXACT, which folds case, whitespace and curly-vs-straight apostrophes (#1611) but NO other punctuation — so a `Trip-Hop` → `Trip Hop` merge is harmless to every genre filter and silences a tag rule spelled `trip-hop`, and a scan running one fold over both stores would call that merge harmless when it is not. The five remaining rule fields name values from vocabularies a genre merge cannot reach. **What is deliberately NOT established is that an orphaned filter matches nothing**: the value may still be caught by another spelling in the vocabulary, and a tag rule reaches three more namespaces, so answering would mean the `json_each` walk that `GET /library/scenes` is fetched-on-expand to avoid (#1570). `remaining` is therefore the rest of that filter's OWN list and the copy claims no more. It is a **warning only**: nothing blocks the merge and nothing rewrites the filter, because genre matching is one-directional and repointing a filter changes what the show MEANS. The blocking twin is `settings/validate.ts assertNoOrphanMoods`, which can refuse — a mood is a closed vocabulary, a genre filter is free text resolved against the library at pick time. Pinned by `scripts/scene-references.test.ts`.
- **Library coverage never counts on the read path (#1570)**: `GET /library/coverage` is polled by the admin Library page, and `coverage.total` comes from `subsonic.iterateAllSongs()` — a `getAlbum` call **per album**, so a few thousand sequential Navidrome requests on a 29k-track library. `library-coverage.get()` is therefore a pure read of the last-known count: it serves DB counts (`countTagged`, `analysedCount`, the vector tallies) plus whatever `total` the last scan found, stamped `scannedAt`. Only `refresh()` walks, and exactly three things call it — the operator's own `POST /library/coverage/refresh` (the panel's Count-library button), and the two ends of a tagger run, which is walking the catalogue anyway: at **start**, guarded on `hasCount()`, because a never-counted library has a null `total` that nulls every percentage on the panel and the hero meter would otherwise read "—" for the whole first run; and at **exit**, where the fresh figure lands. The GET deliberately carries **no `?refresh=1`**: counting is a command, and a read that can start a walk is the shape something eventually polls by accident. The trap this replaces was a 6h staleness check *inside* `get()`: opening the Library page after a controller restart, or six hours after the last visit, silently fired the whole walk, which is the shape the operator sees as "the page checks my library every time I open it". **A library reset is not a walker** — `POST /library/reset` deletes `library.db` and touches nothing in Navidrome, so the `total` a refresh would recompute is already correct; the refresh that used to sit there spent thousands of `getAlbum` calls on the one action an operator least expects to hit the music server. Because nothing recounts unattended any more, the count **persists** to `state/library-count.json` (`{version, total, scannedAt}`, written atomically, a missing or corrupt file reading as "never counted"). Without that, an in-memory cache would blank the total — and every percentage derived from it — on every controller restart, and `scannedAt` could never show an age older than process uptime, which is precisely the staleness the stamp exists to expose. `scanning` and `scanError` are deliberately NOT persisted: they describe this process's attempt, and a stored `scanning: true` from a killed container would spin forever. A **failed** count is recorded rather than only logged (`scanError` on the payload, rendered beside the total), because the scan is fire-and-forget behind a button whose only other feedback is the number appearing — silently failing reads to the operator as nothing having happened; a failed re-count keeps the previous total and its age rather than blanking the panel over a blip. The same reasoning governs the web side (`web/components/admin/library/LibraryContext.tsx`): the coverage query still runs on mount because the read is now cheap, but nothing puts it on a timer except a live tagging run (the % must visibly climb) and an in-flight count (`scanning`, watched at 3s until the new total lands). `total` being null is a first-class state — "not counted yet", not 100% and not an error — and the number always renders with its age, because it is the one figure on that panel that is not live. Pinned by `scripts/library-coverage.test.ts`.
- **Era years (#842)**: a track's era is judged by `show-filter.resolveEraYear`, never raw `year` — the resolved `original_year` wins (walk-time album `originalReleaseDate` on non-compilation albums; MusicBrainz phase-0 enrichment for compilation tracks), and a **compilation's plain `year` is untrusted** (it's the compilation's own release date, so unresolved comp tracks read as unknown-year and drop from era windows). SQL-side era filters (`library-db.filter` yearFrom/yearTo, seed decade bucketing) mirror the same precedence — keep JS and SQL in agreement. `music/musicbrainz.ts` is keyless but hard-throttled to 1 req/s with a descriptive User-Agent (MB policy) — don't widen it; misses are stamped (`original_year_checked_at`) so passes never re-query them. **Every listener-facing surface reads the RESOLVED year, never `song.year`** (#1418): the Liquidsoap annotation (`subsonic.getAnnotatedUri`), the DJ's "Now starting:" line (`llm/internal/prompts/scripts.ts` — this is what the station SAYS), the picker's slim projection (`picker/slim.ts`), and `/now-playing` (`routes/public.ts`, which is where the skins' year comes from — `now-playing.json` carries no year of its own, radio.liq writes title/artist/album/id only). Each had read the raw column, so a reissue anthology had the DJ announcing 2012 over a 1964 Stax single even when the era pipeline had resolved it. Unknown means the field is **omitted, not zeroed** — the same "leave it out rather than assert the wrong decade" rule the era filter follows. Admin surfaces (library table, playlist builder) deliberately keep the raw `year`: the operator is looking at the file. Pinned by `scripts/era-year-surfaces.test.ts`. **The lookup gate is era SUSPICION, not Navidrome's flag** (#1418): keying it on `isCompilation` limited the MusicBrainz pass to 27 tracks out of 27,860 on the reported library, because reissue anthologies arrive as `isCompilation: false`. `music/era-suspect.ts` owns the judgement — a policy module, not a branch in the walk, because the same call is now made from the walk, the lookup gate and the era resolver. It is tuned **precision-first, against a real 9,216-track catalogue rather than by reasoning** — reasoning got it badly wrong, and the first cut flagged 24% of that library. Signals: Navidrome's flag; a Various-Artists album artist; a record with **no lead artist at the front of it** (≥5 distinct *lead* artists, ≥8 tracks, top lead under 30%); a title that says it is a collection (short, specific word list); or a date range in the title that CLOSES before the album's year — the single-artist anthology no artist-count signal can see ("The Atco/Atlantic Singles 1968–1974" on a 2015 release). Three measured findings shaped the artist rule and are pinned as tests: **count `artistRootKey`, not the raw `artist` string** (it carries the features, so "Slauson Boy 2" read as 13 artists and "Mr. Morale" as 9 — both single-artist records; this is the single biggest correctness win in the module); a bare "≥3 artists" rule flags ordinary features-heavy albums wholesale; and **small releases break any ratio test**, since a 4-track EP with four collaborators has no dominant artist by arithmetic alone. The tuned rule lands on 5.9% of that catalogue and reads as DJ mixes, soundtracks, label samplers and various-artist collections. **Re-measure before loosening any of the three numbers.** A false positive costs a 1 req/s MusicBrainz call and can drop a good album out of era shows; a false negative just leaves the old behaviour. The walk logs the blast radius by reason. **Three columns, three jobs**: `is_compilation` is the raw Navidrome FACT, `era_untrusted` the derived verdict, and `yearUntrusted` (composed as `is_compilation = 1 OR era_untrusted = 1`, once, in the row mappers) is the ONLY one era resolution reads — `resolveEraYear`'s third argument is that composed value, and `needsOriginalYearLookup` deliberately refuses the raw flag so a call site can't reopen the gap. Both NULL means "not yet re-walked", which reads as pre-#1418 behaviour exactly. Keep the SQL twins in step: `browse.ERA_YEAR_SQL` and `idsNeedingOriginalYear` carry the same OR. **The walk no longer records an uninformative album tag**: `originalYear` is written only when the album is not suspect AND `originalReleaseDate.year !== year` — an echo of the release year carries no information yet made 18,492 rows look resolved and hid them from the lookup, which skips a non-null `original_year`. Migration 21's `clearEchoedAlbumTagYears` does the same to existing rows and is behaviour-neutral by construction (a trusted album falls through to the identical `year`); it spares a `musicbrainz` or `manual` value that merely coincides with the file year, and spares any `album-tag` year that DIFFERS — those carried real reissue information. Pinned by `scripts/era-suspect.test.ts` (pure, negative cases are the point) + `scripts/manual-original-year.test.ts` (real SQLite, including both directions of the migration). **The operator override is the third and highest-precedence source** (`POST /library/original-year`, `db.setManualOriginalYear`, the `original year · era` field in the library row editor): `original_year_source = 'manual'` outranks both `album-tag` and `musicbrainz`, and **both automatic writers must refuse to touch it** — the walk's upsert (`IN ('musicbrainz','manual')`) and `setOriginalYear`'s WHERE clause. That guard is load-bearing rather than tidy: every walk re-visits every track, so an override the walk can clobber is one the next rescan silently undoes. `originalYear: null` CLEARS the override (year + both stamps back to NULL) and returns the track to the automatic pipeline, rather than pinning "unknown" — "I was wrong about this one" has to be recoverable without a library reset. **The clear only touches rows whose source IS `'manual'`**: the route's `applyToAlbum` loop runs it over every album track, and a sibling holding a `'musicbrainz'` or informative `'album-tag'` year was resolved, not overridden — clearing those in one click would read as unknown-year on every listener surface until a manual enrichment pass. The reconcile walk (`--reconcile-only`) stamps `era_untrusted` and clears stale album-tag years like any walk, so it **chains the same MusicBrainz backfill after its prune** (`enrich.backfillOriginalYears`, shared with phase-0) — without it, the reconcile button performed only the destructive half of #1418 and nothing ever scheduled the lookup that re-answers those tracks; the `checked_at` stamp keeps later reconciles incremental. The override deliberately does **not** invalidate the track's embedding: the `Era:` line is stale until the next retag/tag pass, but dropping the vector to force a refresh would pull the track out of similarity search entirely, and one wrong decade word in a blob carrying genre, tags, lyrics and acoustics is a smaller distortion than a hole in the KNN pool. Era FILTERING reads the column and is correct immediately. Pinned by `scripts/manual-original-year.test.ts` (real SQLite — all three writers are SQL CASE expressions, so a pure test would pass on every possible bug here).
- **Similarity signal (#1246)**: `music/embeddings.formatTrackText` is the canonical embed text and carries **head line + Last.fm tags + lyric excerpt + a `Sound:` line of measured acoustics + an `Era:` decade line** (Sound: zero-shot CLAP `audio_moods`, a tempo BAND from bpm, major/minor from the Camelot `musical_key`, and `instrumental` as a MINORITY marker (`vocal_ranges = []` speaks, vocal tracks don't — a word ~80% of a library shares clusters nothing) — words, never raw numbers or codes, which embed as arbitrary tokens; Era: the decade word from `show-filter.resolveEraYear`, never raw `year`, and being label-derived it deliberately does NOT count as musical signal in `labelOnlyVectors`. The pace curve is deliberately NOT embedded — its practical range is ~0.02–0.5 so fixed thresholds stamp one word on everything, and relative ones break determinism; see `TrackAcoustics`). The tagger's own `moods`/`energy` must **never** go in: phases run enrich → embed → seed → propagate, so those are decided AFTER the vectors exist by voting over the KNN graph built FROM them (`tag-library.ts`), and feeding them back is circular — on a fresh library they don't even exist yet. With every optional line empty the vector IS the label, so similarity ranks by artist/album wording while presenting itself to the picker as mood similarity (a prolific artist self-clusters; "Nick Drake" lands next to "Drake") — `library-coverage.labelOnlyVectors` is what makes that visible, and `enrichment.lastfmTags` defaulting to `null` = auto (fetch only with an API key) is the usual reason a library has none. Changing the text shape means bumping `EMBED_TEXT_VERSION` + `embedding_meta.text_format`, a SOFT advisory (`embeddingFormatStale`) that never blocks a tag run the way a model/dim change does; `resolveIndexTextFormat` records the OLDEST shape still in the index, because a forward run leaves existing vectors alone.
- **Tempo timing is octave-safe at consumption (#1417)**: `librosa.beat.beat_track` can return the DOUBLE on slow material; the measured failures were one-directional, with true 62–88 BPM tracks stored at 124–176. Attempts to choose the octave from onset alternation, `start_bpm` voting and tempogram harmonics did not separate real-audio positives from backbeat controls well enough to ship, so the analyser keeps its raw reading and does **not** force a library re-analysis. `music/mix.ts:timingBpm` instead folds readings at or above 110 downward by octaves before any single-track duration is derived. A true fast track remains musically aligned at half-time (every other beat); a genuine slow reading is never multiplied into a half-bar. The fold is shared by adaptive/ending/washout canvases, washout delay, loop length and chop period, while stored/display BPM, playlist filtering and embedding text remain untouched until an estimator is validated against a real corpus. **Every effect period reaches its audible window by HALVING, never by clamping** — the fix's own first cut clamped after folding, and because the clamp bound over the whole 110–160 band it returned a flat 0.75 s chop gate for the commonest tempi in a library (1.38 beats at 110 rising to 1.90 at 152), so the gate walked the grid and cut mid-note; the washout tap collapsed to a flat 0.45 s for every reading from 110 to 200 and stopped being a dotted eighth of anything. A clamped period is a number inside the window that is a subdivision of nothing. Halving also makes `chopPeriodFor`/`washoutDelayFor`/`loopBarFor` octave-invariant **at any tempo**, so they do not depend on the 110 line at all — only the bar-snapped canvases do, which is why a double landing below 110 (a true 52 read as 104) is left alone rather than guessed at. `scripts/mix-fx.test.ts` pins half/double invariance across every timing consumer AND the grid property itself — each period must be a power-of-two multiple of the raw beat, swept over 30–300 BPM. Equality between two readings alone does not pin this: two equally off-grid values compare equal, which is how the clamp regression passed its own invariance test.
- **Audio-mood calibration + propagated energy (#1362)**: CLAP cosines are **not comparable across moods** — each prompt sits at its own baseline in the shared space, so picking "this track's top-scoring moods" off raw cosines ranks PROMPTS, not tracks (measured on an 11k library: `energetic` fired on 61.7% of tracks and `rainy` on 43.5%, against `calm` 0.9% and `spiritual` 0.2% — a property of the vocabulary, not the music). `music/audio-calibration.ts` is the one place that fixes this: per-mood library-wide mean/sd → `z = (score - mean) / sd`, then the SAME relative top-K selection on the z axis (margin in **standard deviations**, `DEFAULT_MARGIN_SD`, not raw cosine — the old 0.05 has no meaning here). Baselines are computed from **every stored score map, library-wide**, never from a subset: a distribution built only from the tracks the tagger found hardest to read is a biased yardstick. Below `MIN_BASELINE_TRACKS` (200) selection falls back to raw and is byte-for-byte the pre-#1362 path — `topAudioMoods` is now a thin wrapper over `selectAudioMoods(scores, null)` and keeps its own unit test. **The floor is applied PER MOOD, by pruning, not by gating the set** (`prunedBaselines`, the one path `loadBaselines`/`runPropagatedEnergyPass` both take): per-mood `n` diverges — a mood added to the vocabulary since the last full re-score is only scored on tracks analysed after it — so an established mood can sit at n=5000 beside a new one at n=12, and gating on the MAXIMUM `n` (the original reading) let the thin one into the baselines, where its near-degenerate sd is restrained only by `MIN_SD` and its z explodes into a mood that wins every track: the exact #1362 failure, one mood at a time. Requiring EVERY mood to clear the floor is the wrong correction in the other direction — one new vocabulary entry would refuse calibration library-wide. Pruning keeps the library calibrated on its established moods and simply omits the thin one, which `centeredScores` already drops rather than passing raw. **A pass that could not calibrate stamps `UNCALIBRATED_VERSION` (0), never the real one** (`moodStateHashFor` — never call `composeMoodStateHash` from a pass): those labels came from the raw fallback, so the relabel is still owed, and stamping them as current is what made a station that started under the floor and grew past it keep its original tracks on raw selection forever while only post-crossing tracks were ever calibrated. 0 is deliberately the same value a legacy bare hash parses as, because it means the same thing. **The pass is three phases and the order is load-bearing**: score → compute baselines → derive labels. Labels cannot be picked during scoring, because on a full re-score the baselines are a property of the cosines being written by that very loop. A crash between phases leaves `audio_moods` NULL, which `idsNeedingAudioMoods` re-picks up, so the split is resumable rather than lossy. **`audio_embedding_meta.mood_vocab_hash` now carries TWO things joined by `:`** — the vocabulary hash and `CALIBRATION_VERSION` — because they invalidate different amounts of work: a vocabulary change means the *cosines* are stale (full CLAP re-score, needs the analyzer text tower), a calibration change means only the *labels* are (re-derive from disk, `relabelFromStoredScores`, no analyzer at all). Folding the version into the vocab hash instead would strand every install that doesn't run `ANALYZER_HEAVY` — which is most of them — on stale labels. A legacy bare hash parses as version 0, so an upgrade relabels rather than re-scoring. Composite string, so no schema migration. **Energy correction** (`music/propagated-energy.ts`): a `source = 'propagated'` energy is inherited from embedding neighbours, not judged from the track (41% of that library), and where the measured arousal axis — mean z of `HIGH_ENERGY_MOODS` minus mean z of `LOW_ENERGY_MOODS` — is decisive, it overrules the guess. **Deliberately two-sided, never a three-way bucketing**: `audioEnergy` returns `high`/`low`/**null**, and the ambiguous middle returning null is the whole design — bucketing it to `'medium'` would replace one guess with another and call it evidence, whereas null leaves the existing value alone, so a mis-tuned threshold costs coverage rather than correctness. Scope is `source = 'propagated'` ONLY (an `llm`/`manual`/`uncertain-llm` energy is a real per-track decision and is never touched), and the write is the `energy` column ALONE — source/confidence/moods stay as propagation left them, so a corrected row still reports honestly that its MOODS are inherited. The mood names couple this to the shipped vocabulary and moods are operator-editable, hence the degradation: whichever names survive are used, and fewer than 2 on either side yields null rather than a one-sided answer. Runs after `runAudioMoodPass` in `analyze.ts` AND at the end of the tagger's phase 3/4 — one shared path, because a library can gain audio scores long after it was tagged. `ENERGY_HIGH_Z`/`ENERGY_LOW_Z` (±0.35) and `DEFAULT_MARGIN_SD` are defensible starting points, **not measured optima** — they want a pass against a real library. Pinned by `scripts/audio-calibration.test.ts` (pure) + `scripts/propagated-energy.test.ts` (real SQLite; every query here is new SQL that pure tests would pass right through). Issue #1362's third proposal — weighting scores inside the strict `moodLock`/`energyLock` — is deliberately NOT done: those locks are hard with no never-starve on the agent path, so a continuous score there changes starvation behaviour on thin shows and needs its own spec.
- **LLM**: calls go through the AI SDK via the `llm/sdk.js` primitives; `matchRequest` + pool picker use `djObject` (Zod-validated, no manual JSON parsing). Default provider is the homelab Ollama box — reliable but maybe slow; **don't add aggressive retry**. Adding a provider: extend `LLM_PROVIDERS` in `settings.js`, add a builder case in `llm/internal/provider/registry.ts`, and a descriptor in `llm/internal/provider/capabilities.ts` (thinking knob + object strategy); call sites never name a provider, and per-provider quirks live ONLY in `capabilities.ts`.
- **The tagging stamp is a contract version, not the prompt (#1548)**: `prompt_hash` on an LLM-tagged row is `embeddings.promptVocabHash(TAGGER_CONTRACT_VERSION)` — the hand-maintained version in `music/tagger-core.ts` plus the live mood vocabulary, and **nothing from the prompt text itself**. `staleTaggedIds` (`library-db/queries.ts`) re-tags every non-manual row whose stamp differs, and that pass fires only on `--upgrade` / admin Re-scan → **Re-decide moods** — a normal `tag-library` run never consults it. The hash used to take the rendered `taggerBatchSystem()` string, so every cosmetic reword re-tagged the whole library: #1541's transport-wording fix (#1536) would have cost ~1600 batch LLM calls on a 40k library, against the default homelab Ollama box, for a tagging contract that did not change. **This is a behaviour change, and it trades an automatic guarantee for manual discipline.** Before, a semantic prompt edit re-decided old tags by construction; now it re-decides nothing unless someone bumps `TAGGER_CONTRACT_VERSION` — change the mood-selection guidance, the energy scale, the unreadable-track fallback or the batch cardinality rule and you **must** bump it, while a reword, a typo or a reflow must **not**. The loud comment sits on the constant, and `scripts/tagger-contract-hash.test.ts` pins the version, the shipped-vocabulary stamp and the recipe as literals, so any move of the stamp is a diff line in review rather than a surprise on someone's next maintenance pass. Editing `settings.moods` still invalidates on its own (the vocabulary is a hash input, order included) and needs no bump. Landing this invalidated once, like any hash-input change — the first Re-decide after the upgrade re-tags the library, and absorbs #1541 in the same pass. Mirrors `EMBED_TEXT_VERSION`, which does the same job for the embed text.
- **Pick-anchor artist guard** (agent path, #1124/#1187/#1251/#1651): the agent's discovery tools deliberately carry no artist filter (#618 — a strip inside the tools gutted the similarity pool on niche catalogues), so variety is enforced at the point of choice. Each pick cycle captures a **pick anchor**: normally the track on air; for a [pair-aware drain](broadcast.md) deadline pick, the held queue head that the selection is intended to follow. This is a selection snapshot, not a continuously-current FIFO predecessor across the agent's asynchronous tool calls. If the pick repeats the anchor artist, `pickViaAgent` re-picks from the run's own other-artist candidates, escalates to a `blockedArtists` pool call when the run surfaced none, and only then allows the repeat (logged either way). A landed re-pick replaces only the candidate being selected and never removes or rewrites the anchor. **Which candidates the re-pick may choose from lives only in `broadcast/dj-agent/artist-guard.ts`** — never inline at the call site — and it excludes the artists of the `ARTIST_VARIETY_WINDOW` (5) surrounding slots — queued-and-unaired, on air, and last played (`queue.neighbourArtistRoots`) — as well as the rejected artist. Wholly-recent alternatives fall back to that bare rejected-artist exclusion — never starve. Comparison is on `artistRootKey` (`music/recency.ts`), the lead-artist key, so a collaboration can't walk past a guard on the artist fronting it; `artistKey` stays the raw identity key because `trackKey` must keep matching the `title|artist` keys `queue.recentlyPlayed` builds from tag text. At drain time, washout and loop are exit effects stamped from the flagged track's **own analysis**; diagnostics name the real exit owner and the queue successor then known, so selection anchoring cannot retarget the effect. Pinned by `scripts/artist-guard.test.ts`, `scripts/artist-guard-run.test.ts`, `scripts/pair-drain-interleaving.test.ts`, and `scripts/transition-effects.test.ts`.
- **Artist spacing** (agent path, #1406): the guard above originally fired only when the pick matched its anchor; the recency window narrowed the re-pick pool but never the entry condition, so a pick three slots after the same artist was never examined and the window never consulted. Reported live as one artist across a whole morning show, every occurrence legal. `artistGuardCause()` (same module) now returns **which** of two causes fired, and they escalate differently on purpose: `'onair'` is the legacy compatibility cause for a pick-anchor match — events add `basis: 'pick-anchor'` so it is not read as a claim about live position — and keeps the full #1187 cascade (re-pick → `blockedArtists` pool rescue → relax); `'recent'` carries `basis: 'recent-window'`, is a preference and **yields to the run**. If every alternative is also inside the window, or the constrained re-pick does not land, the original pick simply stands. No pool rescue, no second model call chasing spacing, so this can never cost the station a slot or a transition's latency. The window is `settings.llm.artistVarietyWindow` (slots, 0–25, default `ARTIST_VARIETY_WINDOW` = 5; **0 disables spacing only** — pick-anchor protection is not operator-disableable). The composition of those decisions is `runArtistGuard()` in the same module, with the two expensive calls (the constrained re-pick, the pool rescue) INJECTED — `pickViaAgent` keeps every queue and settings read and hands the policy values only. That is what makes the escalation difference testable without a model call; it was previously inline at the call site and verifiable only by reading. Pinned by `scripts/artist-guard-run.test.ts` (every path, counting the injected calls — the spacing cost guarantee IS an assertion that `poolRescue` was never called), `scripts/llm-artist-variety-window.test.ts` (cold-load round trip) and the cause table in `scripts/artist-guard.test.ts`.
- **Known queue/artist follow-up (pre-existing behaviour):** a pair-deadline selection spans awaits. A listener request can append after the held-head anchor is captured and become the selected candidate's actual FIFO predecessor. The guard still compares the candidate with the captured anchor; the request appears only in the surrounding recency snapshot, so a same-artist candidate can take the softer `'recent'` path and be allowed. Fixing that requires an atomic enqueue-time tail revalidation or supersession policy and is a behaviour change outside #1651/#1652. `scripts/pair-drain-interleaving.test.ts` deliberately pauses the real agent selection, inserts a request, and records `held → request → agent-pick` plus the soft guard result until that follow-up lands.
- **Album cooldown** (both paths, #1485 FR 3): `settings.picker.albumHours` — how long a RECORD rests after one of its tracks airs. **Default 0 = off, deliberately**: the artist window already blocks everything an album window shorter than it would, so an album cooldown is only worth anything set ABOVE the artist window, and that number depends on how album-heavy the catalogue is. Off also means an upgrade picks byte-identically. `music/recency.ts` owns the key: **`albumKey`** is the album title folded exactly as `artistRootKey` folds a name, paired with the LEAD of the **album** artist when the source carries one and the track artist otherwise — the artist half is load-bearing, since without it an untagged sampler would block twelve artists off one airing. **Compilations are exempt via the era pipeline's existing composition, never a second heuristic** (#1418): `albumCooldownExempt` reads `isCompilation`/`yearUntrusted` plus the various-artists album-artist marker (`isVariousArtistsName`, moved into `recency.ts` so `era-suspect.ts` and this share ONE list), and an exempt album keys as `''` — which matches nothing, so it neither blocks nor is blocked. Almost nothing carries those flags where it matters, which is what **`music/album-facts.ts` `albumKeyFor`** is for: a raw Subsonic child has no compilation field (OpenSubsonic puts it on the ALBUM), most pool sources return raw children, the agent's `seen` map holds the model-facing `slim()` projection — and must, since `repickFromSeen` serialises those values straight into a prompt, so a field added there is a field the model reads — and a sidecar play row carries only title/artist/album. `albumKeyFor` fills them from `library.getAlbumFacts` (two INTEGER columns off the primary key, NOT `getTrackLite`, which would parse `genres`/`moods` per candidate for nothing) and is **injected** into every consumer — `filterPickerCandidates`'s `albumKeyOf`, `runAlbumGuard`'s deps, `queue.recentAlbumKeys` — so `recency.ts` stays pure and both paths key one catalogue one way. Resolve it or the exemption is dead exactly where it matters (`scripts/album-facts.test.ts`). **One window feeds both paths**: `queue.recentAlbumKeys(hours)` (queued-and-unaired ∪ on air ∪ plays inside the window — the queued side for the reason `neighbourArtistRoots` documents) goes to `filterPickerCandidates`'s `recentAlbums` on the pool path and to `runAlbumGuard` on the agent path, so "too recent" cannot mean two things. Enforcement is at the **point of choice** on the agent path (`broadcast/dj-agent/album-guard.ts`), never in the discovery tools (#618 — around an album track `tracksLikeThis` routinely answers with that album). It is the SOFTEST guard in the sequence and behaves exactly like the artist guard's `'recent'` cause: at most one constrained re-pick, **never** a pool rescue (the pool applies this same cooldown itself, so a rescue re-asks a question already answered at the cost of a model round trip on a transition), and a failed re-pick keeps the pick. It runs AFTER the artist guard, on whatever pick that left standing, and its alternatives step around the same `neighbourArtistRoots` so it cannot walk back into the artist that guard just avoided. On the pool path it is the FIRST stage the starvation cascade drops, and with the set empty the cascade's modes are the exact modes it walked before this existed. `RecentPlay.album` is optional: sidecar rows written by an older build take no part, so an upgrade starts remembering records from the next play rather than guessing. Pinned by `scripts/album-recency.test.ts` (key, exemption, cascade), `scripts/album-facts.test.ts` (the library fill-in, against a real DB), `scripts/album-guard-run.test.ts` (the wiring, with the one-re-pick cost guarantee asserted by counting the injected calls) and `scripts/picker-album-hours.test.ts` (cold-load round trip + the shared window).
- **Full playlist rotation** (both paths, #1612): a per-show `playlistExhaust` — while this show is on, every track in its anchor playlist airs once before any of them repeats. **Default off, and off is byte-identical**: the station-wide `llm.noRepeatWindow` and every other show are untouched. It is a **no-op without `playlistStrict`**, decided that way rather than refused, because a soft anchor may leave the playlist and "every track once" then has no set to be true of — refusing the combination would mean a show that cannot be saved halfway through being configured. The editor only offers the switch behind the strict one, so the dependency is visible there and merely inert in the schema, which is what a hand-edited `settings.json` needs. **The decision lives only in `music/show-recency.ts`** (`showNoRepeatGuard`, which already existed to stop the agent path and the pool picker disagreeing about how wide the hard window is) and reaches the air through `resolveShowShape`'s allowlist — a field missing there is a switch that saves, renders and does nothing (#779). **The window is the rotation's size less TWO, and both slots are load-bearing**: `queue.recentlyPlayedByCount(n)` blocks the ON-AIR track on top of the `n` ended plays it counts (a play is appended to the sidecar when it ENDS), so a window of `n` withholds `n+1` identities; and one identity has to survive, because the count-based guard is checked OUTSIDE the starvation cascade and a window withholding the whole rotation is an empty pool, which is the LLM pick skipped. So `S-2` withholds `S-1` and leaves exactly the track that has waited longest, and the repeat lands on pick `S+1`. `exhaustiveNoRepeatWindow` deliberately skips `effectiveNoRepeatWindow`'s 37.5% library ceiling and its 15-track floor — both tame a number the operator TYPED against a catalogue it was never measured on, and clamping a window derived from its own universe to 37.5% of that same universe only ever means "don't do the thing you were asked to do". **The counted universe is what can AIR**: post-strict-lock, post-exclusion, identity-deduped (two rips of one song consume one slot in the real rotation), and post-`minTrackSec` — the #1573 floor is HARD in the agent's tools, so counting a playlist's 40-second interludes would size the window against a rotation bigger than the one really turning, which under an exhaustive window is the difference between one eligible track and none. The blocklist needs no handling: `subsonic.getPlaylist` already runs `rejectBlocked`, so the anchor arrives clean. **Never-starve**: a rotation too small to leave the headroom yields 0 — the guard switches off and the relaxable cascade takes the show — and it must NOT fall back to the configured `N` instead, which would reintroduce the guard the headroom just refused. Recomputed per pick, so a playlist grown in Navidrome widens the rotation on the next one rather than silently stopping being right. It is NOT part of `autoPlaylistShowKey`: the auto.m3u coast never consults the no-repeat window, so this field cannot change what the fallback contains. **One thing it forced open**: the pool picker's show-playlist source sliced to `CAP_SHOW_PLAYLIST_STRICT` (24) BEFORE the hard set was applied, so an un-pruned random sample of a 40-track anchor missed the one eligible track ~40% of the time and the strict show fell out to its never-starve with nothing in the log to explain it — `sampleShowSource` now prunes hard-blocked tracks before the cap, gated on the guard's own `exhaustive` flag so every other show samples exactly as before, and never-starving twice over (a wholly-blocked list falls back whole, and so does a wholly-recent one). The agent path needed nothing: `collect()` filters before it caps and orders by freshness. Pinned by `scripts/playlist-exhaust.test.ts`, including a rotation simulation over the real `recentlyPlayedByCount` semantics — the only way to show that "40 distinct before a repeat" is what the number actually produces on air.
- **Minimum track length** (#1573): `settings.picker.minTrackLengthSeconds`, with a per-show `minTrackLengthSeconds` override beside `maxTrackSeconds`. **Default 0 = off**, so an upgrade picks byte-identically; a show's `null` means inherit and its `0` is an explicit opt-OUT of a station floor. Asked for by operators whose libraries are full of 40-second skits, interludes and album intros. **It is the CAP's mirror image, not another cap**, and the asymmetry is the whole design: `maxTrackSeconds` is a pure on-air `liq_cue_out` cut, which is why `filterPickerCandidates` says in as many words that track length is *not* a selection criterion — an over-long track stays in the pool and simply crossfades out at the limit. A short track has no equivalent: it cannot be stretched, so the only place to act is the point of choice. Stamping the floor on the annotation beside `maxDurationSec`, or folding it into that comment's "length is not a selection criterion" rule, silently switches the feature off. **`music/track-floor.ts`** owns the policy — a module, not a branch, because the same decision is reached from four places — and takes the same `{ starve }` fork `show-filter.applyStrictLocks` does: **HARD** in the agent's discovery tools (`picker/scope.ts` `collect`, before the playlist lock so a pinned playlist's own interlude drops too), **never-starve** in the pool picker's final selection (`music/picker.ts`, applied to `selectionPool` before `filterPickerCandidates`) and in the auto.m3u coast (`broadcast/scheduler.ts`, beside the strict-playlist and blocklist blocks) — because those two ARE the dead-air scope the hard one falls back into, and "the station must keep making sound" outranks the floor. **An UNKNOWN length passes**: a partly-walked library has rows with no duration, and dropping those would turn a 60s floor into "play only what we happen to have measured"; a `0`/negative/non-finite duration reads as unknown, never as short. Length is read from BOTH field names (`duration` on a Subsonic child, `durationSec` on a library row) — `library.slimTrack`/`songsByMood` already project `durationSec` for the cap, and `library-db.candidateFilterTracks` gained it so the **show-editor candidate diagnostic applies the identical rule the pickers will** (it is NOT gated on `filtersStrict`, so it lands before the strict split and the funnel names it in a warning). `effectiveMinTrackSec` (`settings/persona.ts`) is the one resolver, twinned with `effectiveMaxTrackSec`. **Listener requests are exempt** — an explicit ask is not a pick, and the request scope simply never sets the field. **The key name is deliberately NOT `minTrackSeconds`**: that name is taken by the crossfade-derived floor (`settings/store.ts`, surfaced at `/settings.values.minTrackSeconds`) and means something else entirely — and it is this key's own lower bound, enforced in `update()` where the crossfade is known, on the same reasoning the cap uses (below 2× the crossfade a track gets no solo airtime at all, so a floor under it expresses nothing the mixer does not already impose). **That bound is empirical, not tidiness**: a track whose whole playable span is under the crossfade duration is swallowed by `cross`'s buffer and never airs — it leaves `dj_queue` without a sound and without an error, which is where a 15s stinger requested under an 18s crossfade goes (#1591). Requests are exempt from every length rule the controller owns, so that drop is the MIXER's and the controller cannot see it happen — the resolution probe reports `ready` for any readable file. It is warned about rather than prevented (#1594): `queue.push()` puts one `crossfade` booth-log line in front of the operator when a REQUEST's playable span is under the configured crossfade, and queues the track anyway. The rule lives in `liquidsoap/CLAUDE.md` beside the crossfade rule it belongs to. `0` always passes that check, which is what keeps an untouched station identical. `autoPlaylistShowKey` carries the field, since editing it on the live show changes which tracks the fallback may contain. Pinned by `scripts/track-floor.test.ts` (pure policy, resolver precedence, cold-load round trip, the crossfade bound, the diagnostic funnel, the aliasing contract below), `scripts/show-schema.test.ts` and `scripts/picker-lock-forwarding.test.ts`.

  Three things the first implementation got wrong, each worth keeping in view:

  - **A never-starve filter must never return its INPUT array.** `applyTrackFloor` and `applyStrictLocks` both answer "keep everything" by handing a pool straight back, and the auto.m3u coast rebuilds its pool *in place* (`pool.length = 0; pool.push(...kept)`) — so the rescue cleared the very array it was about to spread back in and wrote an empty `auto.m3u`, turning the last dead-air guard into dead air. Both functions now return a fresh array on every path, and `scheduler.refreshAutoPlaylistInner` funnels all four in-place replacements through one identity-guarded `replacePool` helper. The `applyStrictLocks` half was reachable before the floor existed: a strict show whose only genre resolves to no library tag runs zero lock steps.
  - **"How long is this track?" is answered in ONE place.** `track-floor.trackLengthSeconds` is it, and `recency.durationSeconds` (the cap's reader) delegates to it. It takes the first *usable* value, not the first present one — `duration ?? durationSec` reads a Subsonic child's unmeasured `duration: 0` as a real answer and ignores the library row's true `durationSec` beside it, so the track slips past the floor. The Playlist Builder's `filterByDurationBand` is a deliberate exception, documented at both ends: a recipe band on an operator tool, not a pick-path floor.
  - **Below the crossfade, a track cannot air at all — the lower bound is not a formality.** Measured on the dev stack with `crossfadeDuration: 18`: a 15-second track resolved, queued and reached `next.txt`, but Liquidsoap never took it into `dj_queue` and the reconcile sweep dropped it as stale — *with the floor switched off*, so nothing in this feature was involved. A 31-second track on the same station aired normally. That is the empirical case for `settings.minTrackSeconds()` (`max(30, 2 × crossfade)`) being this key's lower bound rather than a distinct name alone: a floor beneath it could only ever admit tracks the mixer already refuses. It also means an operator who wants 40-second skits gone has nothing to configure below 36s on a default station — those tracks were never going to air.
  - **The diagnostic's INPUT counts stay pre-floor.** `library.indexed` is the indexed library and `playlist.total` is the playlist — the show editor renders the latter as "Playlist anchor: N tracks", and a field labelled with what it counts has to keep counting it. The floor shows up as the drop to the steps below, plus a warning that names it and says the station still plays if it leaves nothing.
- **`artistRootKey` folds verified name variants, not just collaborations** (#1406): a leading article, exact full-name aliases and curly-vs-straight apostrophes. "The Jimi Hendrix Experience" straight into "Jimi Hendrix" walked past the guard because the keys differed, so the known catalogue forms map to one root. Ensemble suffixes are **not stripped generically**: "The Beta Band", "Manchester Orchestra" and "Kronos Quartet" are complete act names, and collapsing them onto Beta/Manchester/Kronos would feed a false equivalence into `blockedArtists`, which `filterPickerCandidates` treats as a hard drop. Add only a verified full-act alias. Normalisation never empties a key ("The Band" → `band`, "The The" → `the`), and article/alias handling runs AFTER the join split so the `X & the Y` exception still sees the tail it protects. Note `queue.recentArtistsSince` is **raw** lowercase, not root keys, and feeds the pool picker's relaxable filter — matched raw against raw, so it does not share this normalisation.
- **Seed vs pick (#1247)**: every pick/request event message hands the agent a real discovery seed's `[id: …]` — the pick event uses the expected predecessor (the queued tail, or the on-air track when the queue is empty), while a request event uses the on-air track — and the discovery tools all EXCLUDE that seed from their own results (`library-db/vectors.ts` `knnByBuffer` drops `excludeId`), so that id is the one real, well-formed track id in the model's context that no tool returned. `util/pick-seed.ts` owns the one wording that says so (`SEED_NOT_A_PICK_CLAUSE`, shared by the pick + request schema `id` descriptions AND `picker-tools.ts`'s empty-result `rule`) — never inline a second copy, and don't weaken it to "never invent an id", which an echoed seed literally satisfies. The trigger is upstream: the harness allows exactly ONE discovery call (`COMMIT_AFTER_STEPS = 1`) before pinning `activeTools: ['done']`, so a seed tool that returns nothing corners the model with an empty `seen` — where BOTH salvage stages are structurally dead (`nearestId` has no keys, `repickFromSeen` returns null on its first line). Hence `buildPickerTools`'s cross-index rescue: `tracksLikeThis`/`tracksThatSoundLikeThis` answer from the OTHER index when the one asked holds no vector for this seed (gated on the primary matching *nothing* — an all-filtered-by-recency primary keeps its `emptyResult`), and it says which index answered so the model doesn't reason about audio neighbours on the text axis. A zero-candidate run does NOT count against the agent circuit breaker (`classifyPickFailure`) when at least one real discovery call ran (`toolCalls > 0`; `flattenToolCalls` excludes the synthetic `done`): the empty set proves this run cannot validate a pick, but does not by itself distinguish index coverage, active filters, an upstream miss, or recovery behaviour, so it is not evidence that the model cannot drive tool calls. Opening the breaker there would kill the session-aware picker for 10 min while advising "switch model". An empty `seen` with ZERO discovery calls means the model never explored (`no-discovery`), which is exactly the breaker's failure class and still counts.
- **Dead-air trim**: some rips carry seconds of silence before the music starts or after it ends, and on air that plays as dead air. The analyzer measures both edges against an **absolute** dBFS floor (`SILENCE_DBFS`, default -50) as `lead_silence_ms` / `tail_silence_ms` (+ `tail_start_ms`, the same tail measurement as an ABSOLUTE offset — a cue_out derived as `duration - gap` inherits every disagreement between the container tag and the decoded file, so the cut moves by whatever the header got wrong), and `music/silence-trim.ts` turns them into `liq_cue_in` / `liq_cue_out` — `radio.liq`'s existing `cue_cut` (added for the #447 length cap) does the cutting, so no mixer change was needed. **The absolute floor is the whole design.** The two measurements that already looked like the answer — `estimate_intro_ms` and `analyze_outro`'s wind-down — are RELATIVE gates gauged against the track's own loud level, which is the right question for "may the DJ still be talking" and the wrong one here: a quiet piano intro clears neither and is unambiguously music, so reusing them would cut into the record. Never wire a relative gate to a cue point. Three guards bound the damage, each for a different failure: the operator's `silenceTrim.minGapMs` floor (a track legitimately opens a beat after zero, and a segued album's inter-track space is deliberate), a fixed `MARGIN_MS` left in place at each edge (the measurement reports the first frame ABOVE the floor, so cutting exactly there lands on the attack), and a `MAX_TRIM_SEC` ceiling so one bad measurement can't halve a song. **Off by default** — it acts on a measurement, and an upgrade must sound byte-identical until the operator asks for it; `ANALYSIS_VERSION` 7 re-targets the library so the columns fill on the next pass. Three consumers share the module and must not drift: the queue drain, the `auto.m3u` rewrite (`broadcast/scheduler.ts`) and `/now-playing`'s track clock — the last one publishes the PLAYABLE span (`drain-policy.playableDurationSec`), because a tagged duration runs every skin's progress bar past the end of the song by exactly the silence that was cut — the fallback playlist is exactly where a divergent second copy goes unnoticed, because nobody is watching when it plays. **A trimmed head shifts every onset the analyzer measured from byte zero**: `shiftOnsetMs` is applied inside `introMsFor` / `firstVocalMsFor` / `introMsOf`, at `applyPairStamps`, and on the bed's ramp budget in `maybePushBed` — that last one is the same vocal onset `firstVocalMsFor` shifts, and leaving it raw made the two disagree about one track (the prompt told the runway was 2s while the bed decision still thought it was 8s and declined a bed the link needed). The tail moves things too: the ending-aware exit canvas measures its wind-down to the trimmed end, not the tagged one, or it counts the silence being cut as part of the ramp. **Every consumer of the trimmed timeline is a `resolveSilenceTrim` call, never a local subtraction.** Both cue points also fold into the queue clock: the playable span is `[cue_in, min(duration, cue_out)]`, so pair-drain deadlines do not count either the skipped head or the cut tail as runway. The tail is measurable only off a file the analyzer proved COMPLETE (a byte-capped download's "tail" is mid-song), so it follows `outro_json`'s COALESCE write rule; head trim works either way. That proof has two different shapes and they are not interchangeable: for a normal track the seek to `duration-20s` IS the proof (a truncated file cannot reach it, and the short-decode check catches it), but the short-track path decodes from offset ZERO, where a file truncated to 70% decodes 70% and clears the same check — so a short track is measured **only** when the caller's `complete` flag says so. A rendered stem seam vetoes on either trimmed edge (`maybeRenderBlend`), because the clip is mixed FROM the outgoing tail and the incoming head: a trimmed successor head would be baked into the clip and air the very silence the trim removes. Pinned by `scripts/silence-trim.test.ts` (the arithmetic, including a cold-load round trip on the settings key), `scripts/silence-trim-library.test.ts` (**the plumbing** — nothing in production hands the resolver a track object carrying the measurements, so `library.get()`'s hand-written projection IS the feature: a column missing from that field list doesn't degrade the trim, it disables it silently with the analysis sitting in the row), `scripts/drain-policy.test.ts`, and `scripts/analyzer_silence_test.py`.
- **Festivals**: operator-editable via admin → Settings → Festivals (`settings.festivals`, validated by `validateFestivalsStrict`; seeded from `FESTIVAL_DEFAULTS` in `settings.ts` only when the key is absent — an emptied list stays empty). Fixed month/day schema, so lunar holidays (Easter, Eid, Lunar New Year) shift year-to-year and need per-year entries. `windowDays` spans month/year boundaries (`getFestivalContext` compares real dates).
- **Listener request hardening** (raid 2026-07-28): every on-air safety decision for requests lives in `util/request-guard.ts` (scripted-opener stripping, verbatim-echo guard on intros/acks, requester-name screening), pinned by `scripts/request-guard.test.ts` — never inline a guard in routes or agents. `settings.requests` (enabled / maxPending / cooldownSec / perIpHourlyCap / globalHourlyCap / onePendingPerIp / repeatCooldownMin) drives `POST /request`'s gates and `middleware/ratelimit.ts`, applies live, and coerces to safe defaults when absent; env `REQUESTS_DISABLED` stays the hard override. Conversational messages are classified by an explicit `kind: 'track' | 'chat'` on **both** paths (`matchRequest`'s `REQUEST_SCHEMA_TOLERANT` and the agent's `requestSchema()`), each with `objectFallbacks: { kind: 'track' }` — chat gets an in-persona text ack, nothing queued. The classification must never be inferred from a null `id` alone: `coerceModelPayload` maps an OMITTED key to null, so "the model forgot the field" and "the listener wasn't asking for music" would be the same wire value, and a real request would silently play nothing. An omitted/botched `kind` degrades to `track` and falls through to the repick salvage + stateless cascade. `languageDirective`/`agentLanguageReminder` now ALWAYS render (default English) with a never-switch clause — the empty-when-unset behaviour is what let session-history mimicry flip the station into Russian; do not restore it. Raw request text survives only in the operator request log; the session, prompts, and air see cleaned text.
- **Likes + the operator heart** (#991, #1253): `state/likes.json` (`broadcast/likes.ts`) is accountless — one record per apparent listener per AIRING, keyed `HMAC(persisted secret, ip)`; the raw IP is never stored and one NAT is one key. The **operator's own heart** (the admin library row action) is a real record under the reserved `listenerKey: 'operator'` + `via: 'operator'`, paired with a synthetic `${songId}|operator` airing key so the existing dedup makes it idempotent per song. It counts in `countForSong` and reaches the picker like any like — the `via` marker exists so surfaces can tell them apart, not so one is second-class. Two exemptions are load-bearing and must not be "simplified" away: an operator record **skips the `topLiked` window cutoff** (a listener like ageing out is a taste snapshot expiring, an operator like ageing out is the DJ forgetting curation the operator set by hand) and **survives the `MAX_RECORDS` trim** (`trimTo` evicts oldest LISTENER records first, falling back to plain oldest-first only when curation alone exceeds the cap, so the bound always holds). Deleting likes never unstars in Navidrome, with ONE deliberate exception: the operator un-hearting when **no likes remain** for the song — that is a toggle, not a purge, and the "none remain" guard is what stops a star earned by twenty listener likes being discarded. Row hearts on every library view read one `GET /likes/index` map rather than per-endpoint annotation, because browse/search/sounds-like come from three different sources and Navidrome search rows have no `library.db` row to annotate. Pinned by `scripts/likes.test.ts`.
- **Blocklist rules** (#1300 FR 1, closes #752): the never-play blocklist carries **rule entries** beside the id entries — attribute predicates (`genre`/`tag`/`mood`/`artist`/`album`/`title`/`playlist` + any-of values) with an optional seasonal **allow-window** (inclusive month/day, wraps year-end, station zone) and an optional show scope — same `blocklist.json`, same routes, same Blocked tab. Pure matching lives in `music/blocklist-rules.ts` (pinned by `scripts/blocklist-rules.test.ts`); `blocklist.ts` owns the evaluation context — a ~15s-memoised active-rule set (clock via `zonedParts`, show via `resolveActiveShow`, both resolved once per sweep, NOT per track) and pre-resolved playlist member sets (the one async input, so `matchOf` stays synchronous). **Enforcement is inherited, not added**: rules ride `hitOf()`/`isBlocked()` inside the existing chokepoints (subsonic reject, library sources, `queue.push` gate, request declines, `annotate`) — never add a second rule-filter at a pick path, that's how the two kinds would drift. `hitOf()` answers entries-FIRST (the UI unblocks exactly the entry), and `BlockRef` is now a `kind: 'entry' | 'rule'` union. Rules are absolute like the id list — no never-starve anywhere, requests included; the row UI deliberately offers **no one-click unblock for a rule ref** (one rule can block hundreds of rows — edits happen on the Blocked tab). `tag` matches EXACT-normalised across every ingested namespace (genres ∪ moods ∪ audioMoods ∪ Last.fm — `show-filter.trackAllTags`); `genre` reuses `genreMatches`' refine direction (blocking "Punk" drops "Punk Rock", not the reverse). Custom file tags (e.g. a country) only work once they land in a field the pipeline ingests — that's an ingestion boundary, not a rules bug. Spec: `docs/superpowers/specs/2026-08-05-exclusion-rules-design.md`.
- **An artist block reads the whole credit, not just the lead** (#1603): a Subsonic song carries one `artistId` — the act it leads with — and `tracks.artist_id` stores that same single id, so before this both the id tier and the exact-name fallback missed a track credited "Y feat. X" and the operator had to block those one by one. `music/recency.ts` `artistParticipantKeys` splits a credit into its acts beside `artistRootKey`, which answers the opposite question (who LEADS it, for the repeat guard), and both halves of the blocklist read it: `matchOf`'s artist tier and `blocklist-rules`' `field: 'artist'` case, whose comment already declared itself to BE the name fallback's semantics. The WHOLE credit is probed first and the acts after it, in ORDER, so a row several artist entries could claim always names the most specific — `matchOf`'s ordering contract, which the admin badge and its one-click unblock depend on. That first probe is the pre-#1603 tier and is load-bearing, not a leftover: `POST /library/blocklist` resolves an artist block from a track row as `{ id: song.artistId, name: song.artist }`, so blocking "this artist" on a `"Host feat. Guest"` row persists the COMPOSITE as the name — a key no participant walk can ever produce. Matching acts alone stranded every such entry, and on the sources with no `artistId` to fall back on (library rows whose `artist_id` predates migration 23, `ruleMatchRows`, queue items) rows the operator had already blocked would have started airing again. The rule half carries the same probe for the same reason: a rule value can be a pasted credit. **The split is `feat.`/`ft.`/`featuring` and nothing else, and that is the design decision, not a first cut**: `&`, `+`, `,` and `x` sit inside act names far more often than they join two credits (Simon & Garfunkel, Hall & Oates, Florence + the Machine, Earth, Wind & Fire, Tyler, the Creator, Chase & Status), and this list is the one place with **no never-starve behind it** — a wrong key silently removes music the operator never blocked, with no starvation fallback to make the loss visible. `FEATURE_SPLIT` already carries the reason it is the exception (always a credit on someone else's track, never part of a name). The cost is honest under-matching — "Y feat. A & B" keys as `["y", "a & b"]`, so a block on A alone does not fire, and a co-credit "A & B" is reachable only through the id tier migration 23 stored it for. A marker at index 0 is not a marker ("Ft. Lauderdale …"), the same guard `artistRootKey` applies, and the bracket cleanup that follows a split drops ONE orphaned closer from the segments after it — never from the lead, whose brackets are part of its name ("Sunn O)))"). The one shared normaliser is `artistNameKey` (case, curly-vs-straight apostrophes, whitespace — `artistRootKey`'s own base fold), used on BOTH sides so a stored entry name and an incoming credit can't disagree; it is deliberately NOT the rest of `artistRootKey`'s folding (article strip, root aliases, join split), which widens a MATCHING key — right for a preference the repeat guard reads as "pick someone else", wrong for a hard drop. **A participant LIST would be the better source and there isn't one**: OpenSubsonic's per-track `artists`/`participants` are not ingested and `tracks` has no column for them, so the display credit is all there is; adding that ingest is the way to reach the shapes this refuses to guess at. Pinned by `scripts/blocklist-featured-artists.test.ts` (the named bands are the collateral cases).
- **One fold under every blocklist name tier** (#1611): the id list's ALBUM tier used to key through a module-local normaliser that folded case and whitespace only, while #1603 moved the ARTIST tier onto `recency.artistNameKey`, which folds curly-vs-straight apostrophes too — so an album entry stored from a row tagged "Guns N’ Roses" missed the same album tagged "Guns N' Roses" and an artist entry did not. Two tiers of one absolute list disagreeing about what one string means. The fold is now `recency.nameKey`, with `artistNameKey` kept as its artist-facing alias (one function, two names — an album title is not an artist, and a site keying one through a helper named for the other is what invites a local copy back in), and BOTH halves of the album key run it: an album title carries an apostrophe as often as a band name ("Sgt. Pepper’s", "Livin’ La Vida Loca"), so folding the artist half alone would move the disagreement one field over rather than close it. **This WIDENS an absolute list** — two spellings of one name collapse to one key, so album entries now block rows the stored spelling missed — which is why it did not ride inside #1608; it is safe for the same reason the artist fold was, since an apostrophe style is which ripper wrote the file and never which record it is. `schemas/blocklist.ts` `normText` — what the `album`/`title`/`tag`/`mood` RULES compile and match with — moved with it, because rules and id entries answer the same question about the same row and a fold on one side only is the same drift one field over; it RESTATES the fold rather than importing it (a mirrored schema module may import only zod) and the two are pinned in step by a table. Nothing else folds: a hyphen still separates `trip-hop` from `trip hop` (which `music/scene-references.ts`' orphan scan depends on), the album key is still a (title, artist) PAIR so a generic "Greatest Hits" cannot cross-match, and there is still no never-starve behind any of it. Pinned by `scripts/blocklist-name-fold.test.ts`.

- **Navidrome 0.64 ID adoption (#1255)**: every authoritative library walk calls `id-rotation.adoptAndPrune` before deleting orphans. A track is adopted only if its canonical ID is present in the live walk. Derived columns, vectors and play attribution move in one SQLite transaction, which also inserts the confirmed map into `id_rotation_journal` (schema 26). Never publish the only copy of that map after committing adoption: a stopped child would delete the old rows and lose the map. The controller replays journal entries through blocklist, likes, recipes and settings; each replay must persist even when a previous failed write already changed its cache. Likes and blocklist saves share one ordered writer per store, including ordinary/debounced saves: atomic rename alone lets an older in-flight snapshot overwrite migrated IDs after the journal has been acknowledged. A failed save rejects its caller without poisoning the writer queue. Boot can read the journal before the library/vector handle opens. Stem-directory moves are replayable and best-effort. Playlist IDs require a live-index match; an unavailable index keeps the map and suppresses post-tag sync. Only successfully persisted mappings are acknowledged, and acknowledgement deletes just the consumed snapshot so a concurrent adoption survives. Legacy `id-rotation.json` handoffs remain readable; malformed/unreadable handoffs hold sync. Recovery tests exercise process exit after DB commit, transaction rollback, later-batch preservation, and failures in all four state stores. Already-pruned derived data requires a pre-migration backup.
